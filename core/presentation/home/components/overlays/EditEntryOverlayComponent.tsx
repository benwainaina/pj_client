import {View} from 'react-native';
import {IEntry} from '../../../../state_manager/home/interfaces';

export const EditEntryOverlayComponent = ({entry}: {entry: IEntry}) => {
  return <View></View>;
};
